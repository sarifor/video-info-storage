extends templates/layout

block content
    if err
        p=err

    if video
        form(method="POST")
            input(type="text", name="title", value=video.title)
            input(type="text", name="desc", value=video.desc)
            input(type="text", name="tags", value=video.tags)
            input(type="submit", value="Edit")