extends templates/layout

block content
    if err
        p=err

    if videos
        each video in videos
            p=a(href=`/videos/${video._id}`)video.title
                //Syntax Error: Unexpected token

    p="home!"